import { Note } from './note';

describe('Note', () => {
  // it('should create an instance', () => {
  //   expect(new Note()).toBeTruthy();
  // });

  // it('should take a title ', () => {
  //   const note = new Note({
  //     title: 'holla',
  //   });
  //   expect(note.title).toEqual('holla');
  // });
});
